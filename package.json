{
  "name": "linkage-designer",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*", "examples"],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
