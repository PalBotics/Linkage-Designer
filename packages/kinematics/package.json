{
  "name": "@linkage-designer/kinematics",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/solver.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
